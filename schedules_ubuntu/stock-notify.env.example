# Copy this to ~/.config/systemd/user/stock-notify.env (NOT inside the repo).
# Pick a hard-to-guess topic name — ntfy.sh public-server topics are world-
# readable by anyone who knows the topic, so use entropy in the suffix.
NTFY_TOPIC=stock-alerts-CHANGE-ME-TO-RANDOM
