# Exact host venv contents, captured 2026-08-01 (Ubuntu 24.04, Python 3.12.3).
#
# requirements-host.txt lists only the deliberate additions on top of "the
# scientific stack the venv already carries" — but nothing recorded what that
# stack actually was, which made the venv unreproducible after a reinstall.
# This file closes that gap. Rebuild with:
#   python3 -m venv venv && venv/bin/pip install -r requirements-host.lock.txt
#
# Regenerate after intentionally adding a package:  venv/bin/pip freeze > requirements-host.lock.txt
exchange_calendars==4.13.2
greenlet==3.5.0
joblib==1.5.3
korean-lunar-calendar==0.3.1
lightgbm==4.6.0
numpy==2.4.6
pandas==3.0.3
pandas_market_calendars==5.2.4
psycopg2-binary==2.9.12
pyluach==2.3.0
python-dateutil==2.9.0.post0
PyYAML==6.0.3
ruff==0.12.12
scikit-learn==1.8.0
scipy==1.17.1
six==1.17.0
SQLAlchemy==2.0.49
threadpoolctl==3.6.0
toolz==1.1.0
typing_extensions==4.15.0
tzdata==2026.2
